notify_all:
  sequence:
    - service: notify.ju
      data_template:
        title: "{{ title }}"
        message: "{{ message }}"
    - service: notify.mo
      data_template:
        title: "{{ title }}"
        message: "{{ message }}"

